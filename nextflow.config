params {
    help = false
    input_dir = ""
    read_pattern = "**.{fq,fastq,fq.gz,fastq.gz}"
    stop_pattern = "{seq*,DONE}.txt"
    process_existing_files = true
    out_dir = "output"
    sample_name = ""
    reference_genome = null
    barcode_pattern = ~/^barcode\d{2}$|unclassified/
    run_folder_pattern = ~/^\d{8}_\d{4}_.+/
}

manifest {
    name = ""
    author = ""
    homePage = ""
    description = ""
    mainScript = "main.nf"
    nextflowVersion = ""
}

executor {
    $local {
        cpus = 4
        memory = "8 GB"
    }
}

labels {
    standard {
        cpus = 2
        memory = "4 GB"
    }
}

profiles {
    standard {
        docker {
            enabled = true
            runOptions = ""
        }
    }
}

timeline {
  enabled = true
  overwrite = true
  file = "${params.out_dir}/execution/timeline.html"
}
report {
  enabled = true
  overwrite = true
  file = "${params.out_dir}/execution/report.html"
}
trace {
  enabled = true
  overwrite = true
  file = "${params.out_dir}/execution/trace.txt"
}
cleanup = true